{
    "node_id": "prism_get_assets_by_project",
    "name": "Prism: Get Assets by Project",
    "description": "Switch to a project then return all its assets.",
    "category": "DCCs",
    "icon_path": "icons/prism_icon.png",
    "inputs": [
        {
            "name": "core",
            "type": "any",
            "widget_type": null,
            "options": null,
            "default": null
        },
        {
            "name": "project",
            "type": "dict",
            "widget_type": null,
            "options": null,
            "default": null
        }
    ],
    "outputs": [
        {
            "name": "assets",
            "type": "list",
            "widget_type": null,
            "options": null,
            "default": null
        },
        {
            "name": "count",
            "type": "int",
            "widget_type": null,
            "options": null,
            "default": null
        }
    ],
    "python_code": "import os\nfrom src.nodes.base import BaseNode\nfrom src.utils.prism_core import resolve_prism_core\n\nclass PrismGetAssetsByProjectNode(BaseNode):\n    name = \"Prism: Get Assets by Project\"\n    category = \"DCCs\"\n\n    def __init__(self):\n        super().__init__(use_exec=False)\n        self.icon_path = \"icons/prism_icon.png\"\n        self.add_input(\"core\", \"any\")\n        self.add_input(\"project\", \"dict\")\n        self.add_output(\"assets\", \"list\")\n        self.add_output(\"count\", \"int\")\n\n    def _project_path(self, project):\n        path = project.get('project_path', '')\n        if not path:\n            cfg = project.get('configPath', '')\n            if cfg:\n                path = os.path.dirname(os.path.dirname(cfg))\n        return path\n\n    def _switch_project(self, core, project):\n        path = self._project_path(project)\n        if not path:\n            self.log_error('Cannot determine project path from project dict.')\n            return False\n        try:\n            core.changeProject(path)\n            return True\n        except Exception as e:\n            self.log_error(f'changeProject failed: {e}')\n            return False\n\n    async def execute(self, inputs):\n        core = resolve_prism_core(inputs)\n        project = inputs.get('project') or {}\n        if core is None:\n            self.log_error('No PrismCore found.')\n            return {'assets': [], 'count': 0}\n        if not project:\n            self.log_error('No project provided.')\n            return {'assets': [], 'count': 0}\n        if not self._switch_project(core, project):\n            return {'assets': [], 'count': 0}\n        try:\n            assets = list(core.entities.getAssets() or [])\n            self.log_info(f\"Found {len(assets)} asset(s) in '{project.get('name', '')}'\")\n            return {'assets': assets, 'count': len(assets)}\n        except Exception as e:\n            self.log_error(f'getAssets failed: {e}')\n            return {'assets': [], 'count': 0}\n\ndef register_node():\n    return PrismGetAssetsByProjectNode\n",
    "use_exec": false
}