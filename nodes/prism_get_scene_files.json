{
    "node_id": "prism_get_scene_files",
    "name": "Prism: Get Scene Files",
    "description": "Lists scene files for an entity/department/task. Wraps core.entities.getScenefiles(entity, department=..., task=...).",
    "category": "DCCs",
    "icon_path": "icons/prism_icon.png",
    "inputs": [
        {
            "name": "core",
            "type": "any"
        },
        {
            "name": "entity",
            "type": "dict"
        },
        {
            "name": "department",
            "type": "string",
            "widget_type": "text",
            "default": ""
        },
        {
            "name": "task",
            "type": "string",
            "widget_type": "text",
            "default": ""
        }
    ],
    "outputs": [
        {
            "name": "scenefiles",
            "type": "list"
        },
        {
            "name": "latest",
            "type": "string"
        },
        {
            "name": "count",
            "type": "int"
        }
    ],
    "python_code": "from src.nodes.base import BaseNode\nfrom src.utils.prism_core import resolve_prism_core\n\nclass PrismGetSceneFilesNode(BaseNode):\n    name = \"Prism: Get Scene Files\"\n    category = \"DCCs\"\n\n    def __init__(self):\n        super().__init__(use_exec=False)\n        self.icon_path = \"icons/prism_icon.png\"\n        self.add_input(\"core\", \"any\")\n        self.add_input(\"entity\", \"dict\")\n        self.add_input(\"department\", \"string\", widget_type=\"text\", default=\"\")\n        self.add_input(\"task\", \"string\", widget_type=\"text\", default=\"\")\n        self.add_output(\"scenefiles\", \"list\")\n        self.add_output(\"latest\", \"string\")\n        self.add_output(\"count\", \"int\")\n\n    async def execute(self, inputs):\n        core = resolve_prism_core(inputs)\n        entity = inputs.get(\"entity\") or {}\n        dept = (inputs.get(\"department\") or \"\").strip().lower()\n        task = (inputs.get(\"task\") or \"\").strip().lower()\n        if core is None:\n            self.log_error(\"No PrismCore found. Add a prism_core_init node to your graph and connect its exec_out before this node.\")\n            return {\"scenefiles\": [], \"latest\": \"\", \"count\": 0}\n        try:\n            files = list(core.entities.getScenefiles(entity) or [])\n            if dept:\n                files = [f for f in files if isinstance(f, dict) and (f.get(\"department\", f.get(\"step\", \"\")) or \"\").lower() == dept]\n            if task:\n                files = [f for f in files if isinstance(f, dict) and (f.get(\"task\", \"\") or \"\").lower() == task]\n            latest = files[-1] if files else \"\"\n            self.log_info(f\"Found {len(files)} scene file(s).\")\n            return {\"scenefiles\": files, \"latest\": str(latest), \"count\": len(files)}\n        except Exception as e:\n            self.log_error(f\"getScenefiles failed: {e}\")\n            return {\"scenefiles\": [], \"latest\": \"\", \"count\": 0}\n\ndef register_node():\n    return PrismGetSceneFilesNode\n",
    "use_exec": false
}