{
    "node_id": "maya_action_scene_info",
    "name": "Maya Action: Scene Info",
    "description": "Creates a Maya action that queries the current scene for frame range, FPS, units, renderer, project path, and scene path. The results are written back into the action dict so they can be read from executed_actions output on the Maya Headless node.",
    "category": "DCCs",
    "icon_path": "icons/maya_icon.jpg",
    "inputs": [
        { "name": "exec_in", "type": "exec" },
        { "name": "actions_in", "type": "list", "default": [] }
    ],
    "outputs": [
        { "name": "exec_out", "type": "exec" },
        { "name": "actions_out", "type": "list" }
    ],
    "python_code": "async def execute(self, inputs):\n    actions_in = inputs.get('actions_in') or []\n    if not isinstance(actions_in, list):\n        actions_in = [actions_in]\n\n    action = {\n        'type': 'scene_info'\n    }\n\n    actions_out = list(actions_in) + [action]\n    self.log_info('Added scene_info action')\n\n    await self.set_output('actions_out', actions_out)\n    await self.set_output('exec_out', True)\n    return {'actions_out': actions_out}"
}
