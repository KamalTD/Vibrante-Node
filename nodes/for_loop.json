{
    "node_id": "for_loop",
    "name": "For Loop",
    "description": "Generates a sequence of numbers. Connect exec_out to start downstream processing.",
    "category": "Operations",
    "icon_path": "icons/gear.svg",
    "inputs": [
        {
            "name": "exec_in",
            "type": "exec"
        },
        {
            "name": "start",
            "type": "int",
            "widget_type": "int"
        },
        {
            "name": "end",
            "type": "int",
            "widget_type": "int"
        },
        {
            "name": "step",
            "type": "int",
            "widget_type": "int"
        }
    ],
    "outputs": [
        {
            "name": "exec_out",
            "type": "exec"
        },
        {
            "name": "then",
            "type": "exec"
        },
        {
            "name": "loop_body",
            "type": "any"
        },
        {
            "name": "current_index",
            "type": "int"
        },
        {
            "name": "indices",
            "type": "list"
        }
    ],
    "python_code": "import asyncio\nasync def execute(self, inputs):\n    start = int(inputs.get('start', 0))\n    end = int(inputs.get('end', 10))\n    step = int(inputs.get('step', 1))\n    \n    indices = list(range(start, end, step))\n    self.log_info(f\"Generated range: {start} to {end}\")\n    \n    # Trigger exec_out explicitly to continue flow to next node (e.g. Loop Body)\n    await self.set_output('exec_out', True)\n    \n    # Trigger then explicitly (backward compatible name for Finished)\n    await self.set_output('then', True)\n    \n    return {'indices': indices, 'current_index': indices[-1] if indices else 0}"
}