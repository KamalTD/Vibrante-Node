{
    "node_id": "deadline_houdini_submit",
    "name": "Deadline: Submit Houdini",
    "description": "Submits a Houdini render job to Thinkbox Deadline by writing a Job Info file and Plugin Info file, then calling deadlinecommand. Specify the ROP output driver path (e.g. /out/mantra1, /out/karma1, /out/arnold). Returns the submitted Job ID.",
    "category": "Deadline",
    "icon_path": "icons/210429_DeadlineFreeOn10RenderMachines.jpg",
    "use_exec": true,
    "inputs": [
        { "name": "exec_in",           "type": "exec" },
        { "name": "deadlinecommand",   "type": "string", "widget_type": "file",     "default": "C:/Program Files/Thinkbox/Deadline10/bin/deadlinecommand.exe" },
        { "name": "hip_file",          "type": "string", "widget_type": "file",     "default": "" },
        { "name": "rop_path",          "type": "string", "widget_type": "text",     "default": "/out/mantra1" },
        { "name": "job_name",          "type": "string", "widget_type": "text",     "default": "Houdini Render Job" },
        { "name": "batch_name",        "type": "string", "widget_type": "text",     "default": "" },
        { "name": "comment",           "type": "string", "widget_type": "text",     "default": "" },
        { "name": "pool",              "type": "string", "widget_type": "text",     "default": "none" },
        { "name": "secondary_pool",    "type": "string", "widget_type": "text",     "default": "" },
        { "name": "group",             "type": "string", "widget_type": "text",     "default": "none" },
        { "name": "priority",          "type": "int",    "widget_type": "int",      "default": 50 },
        { "name": "chunk_size",        "type": "int",    "widget_type": "int",      "default": 1 },
        { "name": "frame_start",       "type": "int",    "widget_type": "int",      "default": 1 },
        { "name": "frame_end",         "type": "int",    "widget_type": "int",      "default": 100 },
        { "name": "frame_step",        "type": "int",    "widget_type": "int",      "default": 1 },
        { "name": "houdini_version",   "type": "string", "widget_type": "dropdown", "options": ["20.5.445", "20.5.278", "20.0.547", "19.5.493"], "default": "20.5.445" },
        { "name": "sim_job",           "type": "bool",   "widget_type": "checkbox", "default": false },
        { "name": "ignore_inputs",     "type": "bool",   "widget_type": "checkbox", "default": false },
        { "name": "auto_workitem",     "type": "bool",   "widget_type": "checkbox", "default": false },
        { "name": "extra_job_args",    "type": "string", "widget_type": "text_area","default": "" }
    ],
    "outputs": [
        { "name": "exec_out",  "type": "exec" },
        { "name": "success",   "type": "bool" },
        { "name": "job_id",    "type": "string" },
        { "name": "stdout",    "type": "string" },
        { "name": "stderr",    "type": "string" }
    ],
    "python_code": "import subprocess, tempfile, os, re\n\ndef _get(inputs, self, key, default):\n    v = inputs.get(key)\n    return v if v is not None else self.get_parameter(key, default)\n\ndef _build_frame_list(start, end, step):\n    if step <= 1:\n        return f'{start}-{end}'\n    return f'{start}-{end}x{step}'\n\ndef _parse_job_id(output):\n    for line in output.splitlines():\n        line = line.strip()\n        if line.lower().startswith('jobid='):\n            return line.split('=', 1)[1].strip()\n    m = re.search(r'[0-9a-f]{24}', output)\n    return m.group(0) if m else ''\n\nasync def _set_outputs(self, result):\n    for k, v in result.items():\n        await self.set_output(k, v)\n    await self.set_output('exec_out', True)\n\nasync def execute(self, inputs):\n    g = lambda key, default: _get(inputs, self, key, default)\n\n    deadline_cmd    = g('deadlinecommand', 'deadlinecommand')\n    hip_file        = g('hip_file', '')\n    rop_path        = g('rop_path', '/out/mantra1')\n    job_name        = g('job_name', 'Houdini Render Job')\n    batch_name      = g('batch_name', '')\n    comment         = g('comment', '')\n    pool            = g('pool', 'none')\n    secondary_pool  = g('secondary_pool', '')\n    group           = g('group', 'none')\n    priority        = int(g('priority', 50))\n    chunk_size      = int(g('chunk_size', 1))\n    frame_start     = int(g('frame_start', 1))\n    frame_end       = int(g('frame_end', 100))\n    frame_step      = int(g('frame_step', 1))\n    houdini_version = g('houdini_version', '20.5.445')\n    sim_job         = bool(g('sim_job', False))\n    ignore_inputs   = bool(g('ignore_inputs', False))\n    auto_workitem   = bool(g('auto_workitem', False))\n    extra_args      = g('extra_job_args', '')\n\n    if not hip_file:\n        msg = 'hip_file is required'\n        self.log_error(msg)\n        result = {'success': False, 'job_id': '', 'stdout': '', 'stderr': msg}\n        await _set_outputs(self, result)\n        return result\n\n    if not rop_path:\n        msg = 'rop_path is required (e.g. /out/mantra1)'\n        self.log_error(msg)\n        result = {'success': False, 'job_id': '', 'stdout': '', 'stderr': msg}\n        await _set_outputs(self, result)\n        return result\n\n    frames = _build_frame_list(frame_start, frame_end, frame_step)\n\n    # --- Job Info ---\n    job_info_lines = [\n        'Plugin=Houdini',\n        f'Name={job_name}',\n        f'Comment={comment}',\n        f'Pool={pool}',\n        f'Group={group}',\n        f'Priority={priority}',\n        f'ChunkSize={chunk_size}',\n        f'Frames={frames}',\n        'EnableAutoTimeout=False',\n        'TaskTimeoutMinutes=0',\n    ]\n    if batch_name:\n        job_info_lines.append(f'BatchName={batch_name}')\n    if secondary_pool:\n        job_info_lines.append(f'SecondaryPool={secondary_pool}')\n    for line in extra_args.splitlines():\n        line = line.strip()\n        if line and '=' in line:\n            job_info_lines.append(line)\n\n    # --- Houdini Plugin Info ---\n    # Extract major.minor from full version string for the Deadline plugin\n    version_short = '.'.join(houdini_version.split('.')[:2])  # e.g. '20.5.445' -> '20.5'\n    plugin_info_lines = [\n        f'Version={version_short}',\n        f'SceneFile={hip_file}',\n        f'OutputDriver={rop_path}',\n        f'SimJob={str(sim_job)}',\n        f'IgnoreInputs={str(ignore_inputs)}',\n        f'AutoWorkitem={str(auto_workitem)}',\n    ]\n\n    job_info_file    = tempfile.NamedTemporaryFile(mode='w', suffix='_job_info.txt',    delete=False, encoding='utf-8')\n    plugin_info_file = tempfile.NamedTemporaryFile(mode='w', suffix='_plugin_info.txt', delete=False, encoding='utf-8')\n    try:\n        job_info_file.write('\\n'.join(job_info_lines))\n        job_info_file.close()\n        plugin_info_file.write('\\n'.join(plugin_info_lines))\n        plugin_info_file.close()\n\n        cmd = [deadline_cmd, job_info_file.name, plugin_info_file.name]\n        self.log_info(f'Submitting Houdini job \"{job_name}\" to Deadline (frames {frames}, ROP={rop_path})')\n        self.log_info(f'Command: {\" \".join(cmd)}')\n\n        proc = subprocess.run(cmd, capture_output=True, text=True, timeout=120)\n\n        job_id = _parse_job_id(proc.stdout)\n        ok = proc.returncode == 0 and ('Result=Success' in proc.stdout or job_id)\n\n        if ok:\n            self.log_info(f'Deadline submission successful. JobID: {job_id}')\n        else:\n            self.log_error(f'Deadline submission failed (exit {proc.returncode}): {proc.stderr or proc.stdout}')\n\n        result = {'success': ok, 'job_id': job_id, 'stdout': proc.stdout, 'stderr': proc.stderr}\n        await _set_outputs(self, result)\n        return result\n    except Exception as e:\n        self.log_error(f'Deadline submit error: {e}')\n        result = {'success': False, 'job_id': '', 'stdout': '', 'stderr': str(e)}\n        await _set_outputs(self, result)\n        return result\n    finally:\n        for f in [job_info_file.name, plugin_info_file.name]:\n            try:\n                os.unlink(f)\n            except Exception:\n                pass"
}
