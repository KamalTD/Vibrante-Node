{
    "node_id": "maya_action_import_obj",
    "name": "Maya Action: Import OBJ",
    "description": "Creates a Maya action to import an OBJ file. Chain multiple action nodes together via actions_in/actions_out, then feed the final actions_out into a Maya Headless node.",
    "category": "DCCs",
    "icon_path": "icons/maya_icon.jpg",
    "inputs": [
        { "name": "exec_in", "type": "exec" },
        { "name": "actions_in", "type": "list", "default": [] },
        { "name": "obj_path", "type": "string", "widget_type": "file", "default": "" }
    ],
    "outputs": [
        { "name": "exec_out", "type": "exec" },
        { "name": "actions_out", "type": "list" }
    ],
    "python_code": "async def execute(self, inputs):\n    actions_in = inputs.get('actions_in') or []\n    if not isinstance(actions_in, list):\n        actions_in = [actions_in]\n\n    obj_path = inputs.get('obj_path') or self.get_parameter('obj_path', '')\n\n    action = {\n        'type': 'import_obj',\n        'obj_path': obj_path\n    }\n\n    actions_out = list(actions_in) + [action]\n    self.log_info(f'Added import_obj action for: {obj_path}')\n\n    await self.set_output('actions_out', actions_out)\n    await self.set_output('exec_out', True)\n    return {'actions_out': actions_out}"
}
